# OBS 素材工作台使用说明

## 功能范围

本版本在 OBS Studio 32.2.1 主程序中内置“素材工作台”，用于正常直播制作中的离线素材整理：

- 批量裁剪、缩放和转码为 MP4。
- 最多叠加两层图片或视频画中画。
- 分别设置画中画位置、大小、出现时间和固定透明度。
- 调整音量、淡入淡出和响度标准化。
- 处理成功后可追加到“媒体播放列表源”，继续使用顺序、随机和循环播放。

本功能不用于规避平台审核、修改媒体指纹或绕过直播平台规则。发布和直播内容仍需遵守平台规则及素材授权要求。

## 开始使用

1. 打开 OBS，在菜单栏选择“停靠窗口 -> 素材工作台”。
2. 点击“添加素材”，可一次选择多个本地视频。
3. 点击“选择输出目录”。程序不会覆盖原素材；同名输出会自动增加编号。
4. 在“处理参数”页设置裁剪、分辨率、帧率、CRF、音频和画中画参数。
5. 返回“任务”页，点击“开始处理”。任务会依次执行，避免同时占满电脑资源。
6. 处理完成后，状态显示“已完成”，输出文件列会显示最终路径。

## 画中画与透明度

1. 在“处理参数”页启用“画中画 1”或“画中画 2”。
2. 点击“选择”添加 PNG、JPG、WebP 或视频素材。
3. “宽度比例”表示画中画宽度占输出画面宽度的比例。
4. “透明度”范围为 0 到 1：0 为完全透明，1 为完全不透明。
5. “出现时间”和“结束时间”以处理后视频时间为准；结束时间为 0 表示持续到结尾。

## 随机循环播放

1. 在 OBS 的“来源”面板中新建“媒体播放列表源”。
2. 在素材工作台的“播放列表”区域点击“刷新”，选择目标源并勾选“完成后加入”。
3. 输出验证成功后，文件会追加到该源末尾；重复路径不会再次添加。
4. 打开“媒体播放列表源”属性，可启用 Shuffle（随机）和 Loop（循环）。

## 任务安全机制

- 原文件始终只读。
- 输出先写入同目录临时文件，FFprobe 验证通过后才重命名为最终文件。
- 损坏输入、FFmpeg 启动失败或编码失败不会阻塞后续任务。
- 点击“取消当前任务”会先请求正常结束，超时后强制停止，并清理临时文件。
- OBS 退出时若仍有任务，会提示留在 OBS 等待或取消任务并退出。

## 常见问题

- **提示找不到 FFmpeg**：请使用完整安装包安装，确认 `data\obs-studio\media-workshop\bin` 下存在 `ffmpeg.exe` 和 `ffprobe.exe`。
- **输出目录不可写**：换到当前账户有写入权限的目录，并确认磁盘空间足够。
- **播放列表为空**：先在 OBS 来源面板创建“媒体播放列表源”，再回到工作台点击“刷新”。
- **处理失败**：把鼠标停在任务状态上可查看错误摘要；OBS 日志中的相关行以 `[media-workshop]` 开头。

## 源码与许可证

- 定制源码：https://github.com/chivukulalaydee-alt/OBS-
- OBS Studio：https://github.com/obsproject/obs-studio
- Media Playlist Source：https://github.com/CodeYan01/media-playlist-source
- FFmpeg：https://ffmpeg.org

安装目录保留了各组件许可证。公开分发修改后的二进制文件时，请同时履行相应 GPL 源码提供义务。
